import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  {
    rules: {
      // This project intentionally uses the standard "fetch in useEffect,
      // track loading/error state" pattern for simplicity (no data-fetching
      // library like React Query). This newer rule nudges toward those
      // heavier tools, which would be over-engineering for a V1 MVP, so it's
      // turned off rather than restructured around.
      "react-hooks/set-state-in-effect": "off",
    },
  },
  // Override default ignores of eslint-config-next.
  globalIgnores([
    // Default ignores of eslint-config-next:
    ".next/**",
    "out/**",
    "build/**",
    "next-env.d.ts",
  ]),
]);

export default eslintConfig;
